{
  "name": "house-of-as-pickleball-mern",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Full MERN Stack Reservation System for House of A's Pickleball Court",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "seed": "cd backend && npm run seed",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently -c \"#34d399,#60a5fa\" \"npm run backend\" \"npm run frontend\" --names=backend,frontend"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.12.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.3",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
